window._BASE_CONFIG = {
  // 项目名称
  title: '模板',
  // 项目后台地址
  baseUrl: 'http://localhost:3003',
  // project key
  projectKey: 'VUE_MALL_ADMIN',
  basePath: '/dist'
}

if (Object.freeze) {
  Object.freeze(window._BASE_CONFIG)
}